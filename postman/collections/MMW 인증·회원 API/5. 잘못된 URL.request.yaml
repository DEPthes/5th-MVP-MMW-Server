$kind: http-request
url: "{{baseUrl}}/api/v1/not-found"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('HTTP 404', () => pm.response.to.have.status(404));
      pm.test('리소스 없음 코드', () => pm.expect(pm.response.json().code).to.eql('RESOURCE_NOT_FOUND'));
    language: text/javascript
order: 5000
